apiVersion: v1
kind: Service
metadata:
  name: {{ include "syntra.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "syntra.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "syntra.selectorLabels" . | nindent 4 }}
